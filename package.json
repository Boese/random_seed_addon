{
  "name": "node-rand",
  "version": "1.0.0",
  "description": "Node JS Addon wrapping c++ rng for seed-random reproducibility",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r ts-node/register test/**/*.spec.ts",
    "build": "cd ./src && node-gyp configure && node-gyp build -j 4 --release && cd ../ && tsc && copy src\\build\\Release\\node_rand.node dist\\ /Y",
    "build:dev": "cd ./src && node-gyp configure && node-gyp build -j 4 --debug && cd ../ && tsc && copy src\\build\\Debug\\node_rand.node dist\\ /Y"
  },
  "keywords": [
    "C++",
    "Addon",
    "NodeJS",
    "Random",
    "Seed",
    "Generator"
  ],
  "author": "Chris Boese",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.7",
    "@types/mocha": "^5.2.7",
    "@types/sinon": "^7.5.1",
    "@types/sinon-chai": "^3.2.3",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "node-addon-api": "^2.0.0",
    "sinon": "^8.0.1",
    "sinon-chai": "^3.4.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "@types/node": "^13.9.0"
  }
}
